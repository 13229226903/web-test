# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Playwright / 测试产物
test-results/
playwright-report/
data/debug/

# 运行日志（任务级，只增不改，本地取证用，不入库）
artifacts/*/journal.jsonl
.claude/journal_task

# 编辑器
.idea/
.vscode/
.DS_Store

# 密钥，勿提交
.env
# 归档后生成的大体积报告与截图，不入库
reports/
data/screenshots/

# 本地 MCP 会话与调试输出
.playwright-mcp/
debug.log

# 项目现存显式备份，按用户要求不推送
artifacts/framework_playwright_mcp_explore_20260901_111335/backups/
# 历史规则快照，不入库
artifacts/runtime/orchestrator.before_*.md
skills/ui-test-page-map-sync/SKILL.before_*.md
# 根目录历史备份文件，不入库
*.bak
*.bak_*

# 探索草稿目录（驱动脚本已移到 scripts/mcp/，其余为一次性探针，按任务归档到 artifacts/<task>/evidence/）
_explore/
