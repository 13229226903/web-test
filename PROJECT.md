# 项目指南（Pokecut — UI 自动化测试）

> 本文件只放「项目级事实与约定」，是所有角色共用的项目常识。
> 各层职责与运行规则不在本文件重复，按需定向读取：
> - 路由 / 状态机 / Gate / 调用上限：`artifacts/runtime/orchestrator.md`
> - 共享红线 / Artifact 接力 / progress / journal：`artifacts/runtime/common.md`
> - 角色职责与输出：`skills/<role>/SKILL.md`
> - 编码规则与技术避坑（selector / 等待 / 组件行为）：`rule.md`

## 项目约定

| 约定 | 本项目取值 |
|---|---|
| 站点基础 URL | 参数化配置：默认 `http://10.17.1.66:3001`（`pytest.ini` 的 `base_url`）；优先级 命令行 `--base-url` > 环境变量 `PYTEST_BASE_URL` > `POKECUT_BASE_URL`（支持 `.env`）> `pytest.ini base_url`；域名变动不写死代码，见下注 |
| 页面地图目录 | `page_map/`（按 `page_map/<模块>/<页面>.yaml` 组织） |
| 测试数据目录 | `data/`（YAML） |
| 测试代码目录 | `tests/`（不嵌套模块子目录） |
| 登录逻辑 | 部分功能需登录（下载、credits 等），匿名可浏览首页和工具页；登录 fixture 见 `conftest.py` |
| 失败 DOM 落盘目录 | `data/debug/`（调试用，已 gitignore） |
| 当前任务指针 | `artifacts/.current_task`（journal 用，任务终态清除） |

## 素材与环境

- 上传素材只能取自 `test_images/` 现有文件，禁止自行生成、下载或临时创建测试图。
- 图片类型 / 分辨率用例按文件命名选择素材：

  | 测试目的 | 选图规则 / 示例 |
  |---|---|
  | 无人脸 | 优先选择文件名包含 `无人脸` 的图片 |
  | 单人脸 / 有人脸 | 优先选择文件名包含 `有人脸` 的图片 |
  | 多人脸 | 优先选择文件名包含 `多人脸` 的图片 |
  | 低分辨率 / 低清 | 优先选择文件名包含 `低分辨率` 的图片 |
  | 1K / 2K / 4K / 8K 分辨率边界 | 优先选择文件名包含 `1K`、`2K`、`4K`、`8K` 的图片；同档多张时优先尺寸/格式最贴近用例的现有文件 |
  | 损坏图 / 异常文件 | 优先选择文件名包含 `损坏` 的图片，只用于异常上传校验，不用于生成态主流程 |

  当前典型素材：`1K.jpg`、`4K.jpg`、`4K.png`、`8K.jpg`、`低分辨率.JPG`、`有人脸.JPG`、`多人脸.jpg`、`无人脸.jpg`、`损坏的图.png`、`文字测例.jpg`。

- 测试服与预部署验证码均固定为 `123456`，直接填写即可，无需点 Send。
- 测试账号按账号态选择：

  | 账号态 / 适用场景 | 邮箱 | 验证码 | 规则 |
  |---|---|---|---|
  | 会员账号（会员权益、订阅态、会员免扣点 / Free icon 等） | `450832596@qq.com` | `123456` | 只用于需要会员态的用例 |
  | 单项购买账号（单项购买权益、单功能授权、单项免扣点 / Free icon 等） | `03201449879@qq.com` | `123456` | 只用于需要单项购买态的用例 |
  | 免费账号（免费次数、新用户、非会员、普通登录态） | 任意未使用邮箱 | `123456` | 需要免费 / 新用户态时随机输入一个邮箱；建议脚本生成唯一邮箱，避免复用污染免费次数 |

  未明确账号态的登录前置优先按用例目的选择；只验证普通登录 / 免费态时用随机免费账号，不要误用会员或单项购买账号改变 UI 预期。

- 后端环境顺序：先在测试服登录并尝试生成；只有测试服生成失败时，才通过目标页 `DEBUG` 面板切到 `预部署`，重新登录并复试；预部署也失败，则在 `sync.md` / page_map 中标记 `skipped` 并写明两边失败现象与证据。
- 购买、订阅、credits / 配额消耗类探索同样遵循「测试服优先、失败再预部署」；回归脚本以 confirmed `cases.md` 声明的环境为准。
- 切换环境后不要复用旧登录态，必须重新走一次登录，确认进入新环境后再开始上传和生成流程。

## 项目背景

- **项目名称**：Pokecut — AI 图片编辑 & 图片生成器
- **技术栈**：Nuxt.js / Vue，SPA 单页应用
- **核心功能**：AI 改图、背景移除/替换、证件照、图片增强、拼贴制作、批量编辑等
- **用户体系**：支持匿名使用 + 注册登录，含 credits 点数系统
- **注意**：页面多为客户端渲染（CSR），Playwright 探索时需等待 JS 渲染完成后再采集元素

## 开发原则

- 所有字段都填（含非必填）、都断言
- 绝不删断言来让测试通过——失败就是 bug
- `expected` 避免用 `"0"`，用有语义的值
- 注释用中文
- selector 优先 ID / role / 稳定属性，禁用 hash class / `:nth-child` / xpath 位置索引（详见 `rule.md`）

## 启动方式

在 Codex 中给主对话一句话即可启动编排；主控会判定 task_type 并写进 state.md。

- 新功能首次提测：把 PRD 走完整流程到归档，PRD 在 `<path>`，入口 `<url>`。
- 功能已上线但缺资产：`<url>` 这个功能已经上线了，帮我补页面地图、用例、脚本，走完整流程到 Allure 报告。
- 已有脚本的功能有改动：更新已归档的回归脚本，跑通并更新 Allure 报告。
- 回归已有归档脚本：回归一下「<功能名>」已登记的脚本，或跑 `tests/<file>.py` 回归并出 Allure 报告。

路由、任务类型、Gate 与角色派发以 `artifacts/runtime/orchestrator.md` 为准；测试命令与 Allure 报告产出以 `skills/ui-test-test-writing/SKILL.md` 为准。
